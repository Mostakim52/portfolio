This folder contains assets for the public site.

Mostakim_Rubaiyat_CV.pdf
- A placeholder PDF was added by the assistant to enable the "Download CV" button in the contact section.
- Replace this file with your actual CV (name it the same or update the link in `src/components/Contact.js`).

To replace:
1. Save your real CV as `Mostakim_Rubaiyat_CV.pdf`.
2. Place it in `public/assets/` (overwrite the placeholder).
3. Rebuild/restart the dev server if it's running.

If you want a different filename, update the href in `src/components/Contact.js` accordingly.